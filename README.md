# Introduction

# Installing

# Usage

+ `--client` or `-c`: run application as client
+ `--server` or `-s`: run application as server

# Commands

+ `delete [file]`
+ `download [file] [folder]`
+ `list`
+ `upload [file]`