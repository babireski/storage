# Introduction

# Installing

# Usage

+ `--client` or `-c`: run application as server
+ `--server` or `-s`: run application as client

# Commands

+ `delete [file]`
+ `download [file] [folder]`
+ `list`
+ `upload [file]`